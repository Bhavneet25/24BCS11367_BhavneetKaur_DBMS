CREATE TABLE Staff (
    Staff_ID NUMERIC PRIMARY KEY,
    Staff_NAME VARCHAR(100),
    Staff_SALARY NUMERIC(10,2),
    DEPT_ID NUMERIC
);

INSERT INTO Staff VALUES (101, 'Rahul', 50000, 10);
INSERT INTO Staff VALUES (102, 'Amit', 60000, 10);
INSERT INTO Staff VALUES (103, 'Priya', 45000, 20);
INSERT INTO Staff VALUES (104, 'Amrit', 60000, 10);
INSERT INTO Staff VALUES (105, 'Pranjal', 85000, 20);
INSERT INTO Staff VALUES (106, 'Puneet', 75000, 20);
INSERT INTO Staff VALUES (107, 'Samar', 40000, 20);

DECLARE
    CURSOR emp_cursor IS
        SELECT Staff_NAME, Staff_SALARY
        FROM Staff
        ORDER BY Staff_SALARY DESC
        FETCH FIRST 5 ROWS ONLY;

    v_name   Staff.Staff_NAME%TYPE;
    v_salary Staff.Staff_SALARY%TYPE;

BEGIN
    OPEN emp_cursor;

    LOOP
        FETCH emp_cursor INTO v_name, v_salary;

        EXIT WHEN emp_cursor%NOTFOUND;

        DBMS_OUTPUT.PUT_LINE(
            'Name: ' || v_name || '  Salary: ' || v_salary
        );
    END LOOP;

    CLOSE emp_cursor;
END;
/
